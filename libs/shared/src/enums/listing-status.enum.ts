export enum ListingStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
}

export enum QueueStatus {
  WAITING = 'WAITING',
  IN_REVIEW = 'IN_REVIEW',
  DONE = 'DONE',
}

export enum ModerationAction {
  APPROVE = 'APPROVE',
  REJECT = 'REJECT',
}
