# Job seed Moderation — à lancer une fois après Auth et Listing.
apiVersion: batch/v1
kind: Job
metadata:
  name: seed-moderation
  namespace: cesi-shop
spec:
  ttlSecondsAfterFinished: 3600
  backoffLimit: 3
  completions: 1
  parallelism: 1
  template:
    spec:
      automountServiceAccountToken: false
      restartPolicy: OnFailure
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z postgres-moderation 5432; do echo waiting for postgres-moderation; sleep 2; done; echo postgres-moderation ready']
        - name: prisma-migrate
          image: YOUR_REGISTRY/cesi-shop-moderation:dev
          workingDir: /app/apps/moderation
          command: ['npx', 'prisma', 'migrate', 'deploy', '--config', './prisma.config.ts']
          env:
            - name: MODERATION_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: database-secrets
                  key: MODERATION_DATABASE_URL
      containers:
        - name: seed
          image: YOUR_REGISTRY/cesi-shop-moderation:dev
          command: ['node', 'dist/apps/moderation/apps/moderation/src/seed-runner.js']
          securityContext:
            runAsUser: 1001
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
              ephemeral-storage: "128Mi"
            limits:
              memory: "256Mi"
              cpu: "300m"
              ephemeral-storage: "256Mi"
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: database-secrets
                  key: MODERATION_DATABASE_URL
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-secrets
                  key: JWT_PUBLIC_KEY
            - name: RABBITMQ_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: RABBITMQ_URL
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: NODE_ENV
                  optional: true
