# Job seed Listing — à lancer une fois après Auth et Media.
apiVersion: batch/v1
kind: Job
metadata:
  name: seed-listing
  namespace: cesi-shop
spec:
  ttlSecondsAfterFinished: 3600
  backoffLimit: 3
  completions: 1
  parallelism: 1
  template:
    spec:
      automountServiceAccountToken: false
      restartPolicy: OnFailure
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z postgres-listing 5432; do echo waiting for postgres-listing; sleep 2; done; echo postgres-listing ready']
        - name: prisma-migrate
          image: YOUR_REGISTRY/cesi-shop-listing:dev
          workingDir: /app/apps/listing
          command: ['npx', 'prisma', 'migrate', 'deploy', '--config', './prisma.config.ts']
          env:
            - name: LISTING_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: database-secrets
                  key: LISTING_DATABASE_URL
      containers:
        - name: seed
          image: YOUR_REGISTRY/cesi-shop-listing:dev
          command: ['node', 'dist/apps/listing/apps/listing/src/seed-runner.js']
          securityContext:
            runAsUser: 1001
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
              ephemeral-storage: "128Mi"
            limits:
              memory: "256Mi"
              cpu: "300m"
              ephemeral-storage: "256Mi"
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: database-secrets
                  key: LISTING_DATABASE_URL
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-secrets
                  key: JWT_PUBLIC_KEY
            - name: RABBITMQ_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: RABBITMQ_URL
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: NODE_ENV
                  optional: true
